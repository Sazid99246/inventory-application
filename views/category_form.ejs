<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h1>
        <%= title %>
    </h1>
    <form method="POST">
        <label>Name: <input type="text" name="name" value="<%= category ? category.name : '' %>" required></label><br>
        <label>Description: <textarea
                name="description"><%= category ? category.description : '' %></textarea></label><br>

        <% if (category) { %>
            <p>Enter Admin Password to confirm changes:</p>
            <input type="password" name="admin_password" required><br><br>
            <% } %>

                <button type="submit">Submit</button>
    </form>
    <a href="/categories">Cancel</a>
</body>

</html>